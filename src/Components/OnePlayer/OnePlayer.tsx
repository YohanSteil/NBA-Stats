import React, { useState } from 'react';
import axios from 'axios';

const OnePlayer = () => {

    // const [onePlayer, setOnePlayer] = useState([])

    // const fetchOnePlayer = await axios.get ('https://api.balldontlie.io/v1/stats')

    return (
        <div>
            <h1>Stat du joueur</h1>
        </div>
    );
};

export default OnePlayer;